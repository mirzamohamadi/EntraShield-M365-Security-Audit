# Screenshots

This folder intentionally uses SVG for the first public preview image because it renders well on GitHub and does not require any real tenant data.

Current preview:

![Sample Dashboard](../reports/screenshots/sample-dashboard.svg)

When adding real screenshots, sanitize tenant names, usernames, domains, object IDs, IP addresses, and timestamps.
